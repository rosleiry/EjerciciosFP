#include <stdio.h>
#include <stdlib.h>

/*3. Vuelva a reescribir las funciones de ordenamiento del portafolio pasado de manera más genérica.
Estas funciones deben de recibir la función de comparación y debe de funcionar para cualquier tipo de datos.
El prototipo debería de ser algo así:
void sort(void *base, size_t nitems, size_t size, int (*compar)(const void *, const void*))*/

int main()
{

}
